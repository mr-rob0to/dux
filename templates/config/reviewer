# The reviewer /ship runs for step 6, the pull request's one code review. It is
# a command line; the review prompt is appended to it as one argument.
# Copy this file to config/reviewer to change it: /ship reads config/ first and
# falls back here, so a fresh clone runs the gate on this line.
# If the model named here is refused, fall back to another one and say in the
# pull request which reviewer actually ran. A review that silently downgraded is
# worse than one that did not happen.
codex exec -m gpt-5.6-sol --sandbox read-only
